import 'package:flutter/material.dart';

class SwipesScreen extends StatelessWidget {
   
  const SwipesScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('SwipesScreen'),
      ),
    );
  }
}